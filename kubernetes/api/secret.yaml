apiVersion: v1
kind: Secret
metadata:
  name: rag-secrets
  namespace: rag-system
type: Opaque
data:
  # Values must be base64 encoded:
  # echo -n "sk-your-key" | base64
  # WHY base64? Not encryption — just encoding.
  # Use AWS Secrets Manager for real secrets.
  OPENAI_API_KEY: ""     # Leave empty if using Ollama